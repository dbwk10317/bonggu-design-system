Badge · 미읽음·대기 건수. 0이면 렌더하지 않는다. 상태 문구는 StatusPill, 분류는 Tag, 숫자만 Badge.

```jsx
<Badge count={12} tone="crit"><IconButton icon="bell" aria-label="알림" /></Badge>
<Tabs … /> 탭 카운트는 Tabs의 count prop을 쓴다
<Badge count={3} />
```
