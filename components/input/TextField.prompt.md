TextField — 한 줄 입력. Field 안에서 라벨을 받는다. 단위는 suffix, 수치는 mono.

```jsx
<Field label="임계 온도"><TextField mono suffix="°C" defaultValue="75" fit="fixed" width={140} /></Field>
<TextField icon="user" placeholder="이름" />
```
