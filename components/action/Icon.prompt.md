Icon — Phosphor Bold 아이콘 하나. 장식이면 label 없이(aria-hidden), 단독 의미면 label.

```jsx
<Icon name="pulse" />
<Icon name="warning" size={18} label="주의" />
```
리액트 앱에서는 `@phosphor-icons/react`의 같은 이름 아이콘을 `weight="bold"`로 쓴다. 손으로 그린 SVG 아이콘 금지(마스코트만 예외).
