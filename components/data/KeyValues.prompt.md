KeyValues · 카드 안 키·값 3~5행. 수치는 자동 mono, 결측은 "수집 안 됨"으로 mono를 벗고 흐리게.

```jsx
<KeyValues rows={[["온도","61.5°C"],["로드 (1m)","2.41"],["실행 중",null]]} />
```
- 결측은 값에 `null`을 넣는다(`undefined`·`NaN`도 같다). 문구를 직접 넣어도 같게 그리지만 `null`이 기본이다.
- 빈 문자열은 수집된 빈 값이라 빈 칸으로 남는다.
