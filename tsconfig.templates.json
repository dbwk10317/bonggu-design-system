{
  "//": "대시보드 템플릿이 컴포넌트 공개 API를 제대로 쓰는지 본다. 목 데이터는 조립 예시의 재료라 noImplicitAny를 끈다(eslint templates 블록과 같은 태도). support.js는 dc-runtime 생성물이라 제외한다.",
  "extends": "./tsconfig.json",
  "compilerOptions": { "noImplicitAny": false },
  "include": ["templates/**/*.jsx", "templates/**/*.js", "templates/**/*.d.ts", "types-dom.d.ts"],
  "exclude": ["templates/dashboard/support.js"]
}
