{
  "//": "Checks that the dashboard templates use the public component API correctly. Mock data is assembly-example material, so noImplicitAny is off (same stance as the eslint templates block). support.js is a dc-runtime artifact and is excluded.",
  "extends": "./tsconfig.json",
  "compilerOptions": { "noImplicitAny": false },
  "include": ["templates/**/*.jsx", "templates/**/*.js", "templates/**/*.d.ts", "types-dom.d.ts"],
  "exclude": ["templates/dashboard/support.js"]
}
