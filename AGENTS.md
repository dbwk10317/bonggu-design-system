# 규칙의 출처

디자인 규칙은 두 곳에만 있다.

- `readme.md` 사람이 읽는 규칙 전문. 시각·카피 규범, 결측·fit·접근성 계약, 동작 계약, 컴포넌트 선택 가이드, 파일 Index.
- `tests/rule-regressions.cjs` 그 중 기계로 판별할 수 있는 항목의 검사.

이 두 곳 밖에는 규칙이 없다. 코드 주석, `.prompt.md`, `.d.ts`, `guidelines/*.html`, 템플릿 README, 테스트 파일의 주석은 규칙의 출처가 아니다. 이들이 규칙처럼 읽히는 문장을 담고 있으면 그것은 규칙이 아니라 중복이며, 지우고 `readme.md`를 가리킨다.

- 규칙을 새로 정하려면 `readme.md`에 적는다. 다른 파일에 적은 규칙은 효력이 없다.
- 기계로 판별할 수 있는 규칙은 `tests/rule-regressions.cjs`에 검사를 함께 넣는다. 검사가 없는 규칙은 문서에만 있는 상태임을 알고 쓴다.
- 두 곳이 어긋나면 `readme.md`가 기준이고 검사를 고친다.
- 구현이 `readme.md`와 어긋나면 구현을 고친다. 주석이나 별도 문서로 예외를 만들지 않는다. 예외가 정말 필요하면 `readme.md`에 예외로 적는다.
- 코드 주석은 규칙을 옮겨 적지 않는다. 왜 이렇게 썼는지만 한 줄로 남기고 규칙은 `readme.md`에 둔다.

# 변경 원칙

증상만 제거하는 예외 코드보다 원인을 먼저 해결한다.

1. 수정 전에 `readme.md`, 해당 컴포넌트의 `.d.ts`와 `.prompt.md`, 실제 사용처를 비교한다.
2. 문제를 잘못된 규칙, 구현의 계약 위반, 공통 구조 부재로 구분한다. 기준이 충돌하면 먼저 `readme.md`에서 의도된 동작을 확정한다.
3. 원인이 같은 동작은 공통 구조에서 수정한다. 특정 화면 이름, 강제 리마운트, 임의 지연, CSS 특이성 증폭으로 문제를 숨기지 않는다. 단, 문서화된 세션 경계에서 상태를 초기화하는 것은 허용한다.
4. 토큰·클래스·공통 헬퍼를 새로 선언하면 사용처를 같은 변경에 포함한다. 선언만 있고 소비처가 없는 이름은 규칙이 아니라 미사용 잔재다. 같은 값을 여러 곳에 리터럴로 적는 대신 정본 토큰을 참조한다.
5. 문서·타입·구현을 함께 맞춘다. 외부 소비자가 받는 값이나 이벤트 시점이 바뀌면 그 영향을 결과 보고에 적는다.
6. 실제 재현 조건을 회귀 테스트에 남기고, 공통 구조 변경은 중첩·재개방·외부 값 변경을 함께 검증한다. 실행법은 `tests/README.md`를 따른다.
7. 소스 변경 후 번들을 다시 생성한다. 결과 보고에는 원인, 기준 규칙, 수정 방식, 기존 사용처 영향, 검증 결과를 포함한다.

# 컴포넌트를 만들거나 고칠 때

컴포넌트 작업은 규칙을 확인하는 것으로 시작하고 문서를 갱신하는 것으로 끝난다.

1. 먼저 `readme.md`의 해당 규범과 `tests/rule-regressions.cjs`의 검사를 읽는다. 컴포넌트가 규칙을 새로 만들지 않는다.
2. 필요한 규칙이 `readme.md`에 없으면 구현을 먼저 하지 않는다. `readme.md`에 규칙을 정하고, 기계로 판별할 수 있으면 `tests/rule-regressions.cjs`에 검사를 함께 넣은 뒤 구현한다.
3. 구현을 고쳤으면 같은 변경에서 그 컴포넌트의 `.d.ts`와 `.prompt.md`를 갱신한다. 공개 prop, 기본값, 예시가 실제 구현과 어긋난 상태로 남기지 않는다.
4. 컴포넌트를 추가하거나 지웠으면 `readme.md`의 Index, 컴포넌트 목록과 개수, 선택 가이드를 함께 맞춘다.
5. 규칙 자체를 고쳤으면 그 규칙을 쓰는 다른 컴포넌트와 `guidelines/*.html`의 값도 같이 확인한다.

# 파일의 역할

규칙은 `readme.md`에만 있다는 전제 아래, 나머지 파일이 담을 수 있는 것은 다음뿐이다.

- `AGENTS.md` 규칙의 출처, 변경 원칙, 이 역할표.
- `CLAUDE.md` Claude Code 실행 환경. 경로·셸·빌드·검증 명령만.
- `SKILL.md` 스킬 진입점. `readme.md`를 가리키기만 한다.
- `tests/README.md` 검증 실행법과 게이트가 덮는 범위.
- `guidelines/*.html` 확정된 값을 눈으로 확인하는 카드. 값을 보여주고 규칙을 새로 쓰지 않는다.
- `components/<Name>.d.ts` 공개 타입 시그니처.
- `components/<Name>.prompt.md` 그 컴포넌트의 사용 예시. 시스템 전체에 적용되는 규칙은 여기 적지 않는다.
- `templates/dashboard/README.md` 템플릿 파일이 어떤 화면을 재조립한 것인지에 대한 목록.

`_ds_bundle.js`, `_ds_manifest.json`, `_adherence.oxlintrc.json`은 생성물이다. 손으로 고치지 않고 출처(`components/**`, `tokens/*.css`)를 고친 뒤 `node build-bundle.mjs`를 다시 실행한다. adherence 설정에는 손으로 유지하는 항목이 섞여 있고, 첫 키 `x-generated`가 그 경계를 적어 둔다.
