# 변경 원칙

증상만 제거하는 예외 코드보다 원인을 먼저 해결한다.

1. 수정 전에 readme, 해당 컴포넌트의 .d.ts와 .prompt.md, 실제 사용처를 비교한다.
2. 문제를 잘못된 규칙, 구현의 계약 위반, 공통 구조 부재로 구분한다. 기준이 충돌하면 먼저 의도된 동작을 명시한다.
3. 원인이 같은 동작은 공통 구조에서 수정한다. 특정 화면 이름, 강제 리마운트, 임의 지연, CSS 특이성 증폭으로 문제를 숨기지 않는다. 단, 문서화된 세션 경계에서 상태를 초기화하는 것은 허용한다.
4. 문서·타입·구현을 함께 맞춘다. 외부 소비자가 받는 값이나 이벤트 시점이 바뀌면 호환성 영향을 명시한다.
5. 실제 재현 조건을 회귀 테스트에 남기고, 공통 구조 변경은 중첩·재개방·외부 값 변경을 함께 검증한다. 실행법은 tests/README.md를 따른다.
6. 소스 변경 후 번들을 다시 생성한다. 결과 보고에는 원인, 기준 규칙, 수정 방식, 기존 사용처 영향, 검증 결과를 포함한다.

현재 계약 결정은 docs/behavior-contracts.md에 기록한다.
