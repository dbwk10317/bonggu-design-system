---
"@dbwk10317/bonggu-design-system": minor
---

사진 목록 컴포넌트 `JustifiedGallery`를 추가합니다. 사진마다 원본 비율을 지키면서 마지막 행을 뺀 모든 행을 컨테이너 폭에 맞춰 채웁니다. `onOpen`을 주면 타일이 사진을 여는 버튼이 되고, `footer` 슬롯에 더 불러오기 같은 동작을 둡니다. 원본 크기가 없는 사진은 1:1로 배치합니다. 한 장을 대표로 보이는 자리는 계속 `AspectRatio`(4:3)를 씁니다.
