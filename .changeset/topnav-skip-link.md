---
"@dbwk10317/bonggu-design-system": minor
---

`TopNav`에 `skipTo` prop을 추가합니다. 본문 요소의 id를 넘기면 본문으로 건너뛰는 링크를 첫 탭 스톱으로 냅니다. 평소에는 보이지 않고 초점을 받으면 나타납니다. `TopNav`를 쓰는 화면은 본문에 `id`와 `tabIndex={-1}`을 주고 그 id를 넘깁니다.
